/**
 * Auth route group layout
 * Wraps authentication screens with common layout
 */

import { Stack } from 'expo-router';

export default function AuthLayout() {
  return (
    <Stack>
      {/* TODO: Add common auth layout elements if needed */}
    </Stack>
  );
}
